<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#1e3a5f">
  <title>Başarılı - BEST Freight</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      background: linear-gradient(135deg, #1e3a5f 0%, #0f1f35 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      color: #e5e7eb;
    }
    
    .container {
      width: 100%;
      max-width: 400px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 20px;
      padding: 40px 30px;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
      text-align: center;
    }
    
    .success-icon {
      font-size: 80px;
      margin-bottom: 20px;
      animation: bounce 0.6s ease;
    }
    
    @keyframes bounce {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.1); }
    }
    
    h1 {
      font-size: 24px;
      font-weight: 700;
      color: #10b981;
      margin-bottom: 12px;
    }
    
    .message {
      font-size: 16px;
      color: #cbd5e1;
      line-height: 1.6;
      margin-bottom: 20px;
    }
    
    .file-count {
      display: inline-block;
      background: rgba(16, 185, 129, 0.15);
      border: 1px solid rgba(16, 185, 129, 0.3);
      border-radius: 8px;
      padding: 10px 20px;
      font-size: 14px;
      color: #34d399;
      margin-bottom: 25px;
    }
    
    .position-info {
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: 12px;
      padding: 12px 16px;
      margin-bottom: 25px;
    }
    
    .position-info .label {
      font-size: 11px;
      color: #94a3b8;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .position-info .value {
      font-size: 16px;
      font-weight: 600;
      color: #60a5fa;
      margin-top: 4px;
    }
    
    .thank-you {
      font-size: 14px;
      color: #94a3b8;
    }
    
    .upload-again-btn {
      display: inline-block;
      width: 100%;
      padding: 14px 24px;
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      border: none;
      border-radius: 12px;
      color: white;
      font-size: 15px;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.3s ease;
      margin-top: 20px;
    }
    
    .upload-again-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(59, 130, 246, 0.3);
    }
    
    .footer {
      margin-top: 30px;
      font-size: 11px;
      color: #64748b;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="success-icon">✅</div>
    
    <h1>Yükleme Başarılı!</h1>
    
    <p class="message">
      Teslim CMR(leri) başarıyla sisteme yüklendi.
    </p>
    
    <div class="file-count">
      📄 <%= fileCount %> Teslim CMR'si yüklendi. Lütfen operasyon ekibine bilgi veriniz.
    </div>
    
    <div class="position-info">
      <div class="label">Pozisyon No</div>
      <div class="value"><%= positionNo %></div>
    </div>
    
    <p class="thank-you">
      Teşekkür ederiz. Bu sayfayı artık kapatabilirsiniz.
    </p>
    
    <a href="/driver-upload/<%= token %>" class="upload-again-btn">
      📷 Tekrar Teslim CMR Yükle
    </a>
    
    <div class="footer">
      BEST Freight ERP, Created by Aytuğ Yayman &copy; 2025
    </div>
  </div>
</body>
</html>
