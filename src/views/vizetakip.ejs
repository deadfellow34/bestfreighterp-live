<%- include('./partials/navbar') %>

<style>
  /* Sayfanın geri kalanını (navbar hariç) komple kaplasın */
  .vize-page {
    position: fixed;
    inset: 56px 0 0 0;     /* top:56px (navbar yüksekliği), left/right/bottom:0 */
    display: flex;
    flex-direction: column;
    background: radial-gradient(circle at top, #1f2937 0, #020617 40%, #020617 100%);
  }

  .vize-header-block {
    padding: 8px 16px;
    flex-shrink: 0;
  }

  .vize-header { margin: 0; color: #f8fafc; font-size: 20px; font-weight: 600 }
  .vize-desc   { margin: 0; color: #cbd5e1; font-size: 13px }

  .vize-frame-wrap {
    flex: 1;
    min-height: 0;
    display: flex;
  }

  .vize-frame-wrap iframe {
    flex: 1;
    width: 100%;
    height: 100%;
    border: 1px;          /* istersen 1px border geri koyabilirsin */
  }

  .vize-note {
    padding: 4px 16px 8px;
    font-size: 0.9em;
    color: #9ca3af;
    flex-shrink: 0;
  }
</style>

<div class="vize-page">
  <div class="vize-header-block">
    <h2 class="vize-header">Vize Takip</h2>
    <p class="vize-desc">
      Bu sayfa Google Sheets üzerinde yayınlanmış bir <em>Vize Takip</em> dökümanını içerir.
    </p>
  </div>

  <div class="vize-frame-wrap">
    <iframe
      src="<%= sheetUrl %>"
      title="Vize Takip - Google Sheet"
      loading="lazy"
      frameborder="0"
      allowfullscreen
    ></iframe>
  </div>

  <div class="vize-note">
    Eğer iframe görünmüyorsa, Google Sheets yayımlama ayarlarınızı kontrol edin.
  </div>
</div>
